version: '3.8'

services:

  db:
    image: mongo:6.0
    volumes:
      - db:/data/db

  giuseppe:
    image: ghcr.io/niceaesth/giuseppe:master
    container_name: giuseppe
    volumes:
      - ./config.json:/home/appuser/config.json
    depends_on:
      - db

volumes:
  db:
    driver: local
